network:
  version: 2
  renderer: networkd
  ethernets:
    virtio-nic:
      match:
        macaddress: ${mac_address}
      addresses:
        - ${ip_address}/24
      routes:
        - to: 0.0.0.0/0
          via: 172.16.1.1
      nameservers:
        addresses:
          - ${dns_server}
